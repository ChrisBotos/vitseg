###############################################################################
# requirements.txt  –  compatible with Python 3.10
# Updated with verified latest compatible versions as of July 2025
#
# GPU build (CUDA 12.1):
#     pip install -r requirements.txt \
#        --extra-index-url https://download.pytorch.org/whl/cu121
#
# CPU-only build:
#     pip install -r requirements.txt
###############################################################################

# ---------- Core numerical stack --------------------------------------------
numpy==2.2.6               # Latest version supporting Python 3.10 (verified)
scipy==1.14.1              # Latest version supporting Python 3.10 (verified)
pandas==2.2.3              # Latest stable version compatible with Python 3.10

# CuPy binary wheel for CUDA 12.x
cupy-cuda12x==13.5.0       # Stable version for CUDA 12.x

# Lock object used by CuPy
fastrlock>=0.5              # Minimum required version

# ---------- Arrow / parquet I/O ---------------------------------------------
pyarrow==20.0.0            # Stable version compatible with Python 3.10
fastparquet==2024.2.0      # Stable version compatible with Python 3.10

# ---------- Plotting & imaging ----------------------------------------------
matplotlib==3.10.3         # Latest version compatible with Python 3.10
contourpy==1.1.0           # Compatible version
pillow==10.0.1             # Compatible with imageio requirements (<10.1.0)
imageio==2.33.0            # Required by scikit-image 0.23.2 (>=2.33)
scikit-image==0.23.2       # Latest version compatible with Python 3.10
pywavelets==1.8.0          # Compatible version
imagecodecs==2025.3.30     # Compatible version

# ---------- Machine-learning -------------------------------------------------
scikit-learn==1.7.0        # Latest version compatible with Python 3.10
joblib==1.5.1              # Compatible version
tqdm==4.67.1               # Latest stable version

# ---------- Deep-learning stack ---------------------------------------------
# CPU-only versions (default for compatibility):
torch==2.7.1               # Latest stable version - verified Python 3.10 support
torchvision==0.22.1        # Updated to match torch version
# For GPU support, install manually after environment setup:
# pip install torch==2.7.1+cu121 torchvision==0.22.1+cu121 --extra-index-url https://download.pytorch.org/whl/cu121

# ---------- Single-cell & AnnData -------------------------------------------
anndata==0.11.4            # Latest version compatible with Python 3.10
scanpy==1.11.2             # Latest version compatible with Python 3.10

# ---------- NLP / Transformers ---------------------------------------------
transformers==4.52.4       # Latest version compatible with Python 3.10

# ---------- Dev / testing ----------------------------------------------------
pytest==8.4.0             # Latest version compatible with Python 3.10
psutil==7.0.0              # Latest stable version
