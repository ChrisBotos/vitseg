###############################################################################
# requirements.txt
# ----------------
# This lock-file keeps pip’s dependency resolver shallow, avoids the
# “resolution-too-deep” error, and guarantees binary wheels exist for
# Python 3.11 as of June 2025.
#
# If you do not have CUDA 12.1, simply comment out the two ‘+cu121’ lines
# or replace them with ‘+cpu’.
###############################################################################

######## Core array / dataframe stack #########################################
numpy==2.2.0           # First NumPy major that is widely ABI-compatible.
scipy==1.15.1          # Built against NumPy ≥2.2 wheels.
pandas==2.3.0
pyarrow==21.1.0
fastparquet==2024.11.0

######## Plotting and I/O #####################################################
matplotlib==3.10.3
contourpy==1.1.0       # Explicit pin keeps Matplotlib happy.
pillow==11.2.1
imageio==2.32.1        # Matches scikit-image 0.23.x wheel requirement.

######## Machine-learning core ###############################################
scikit-learn==1.7.0
joblib==1.5.1
tqdm==4.67.1

######## Deep-learning stack ##################################################
# Use the official PyTorch extra index for CUDA wheels:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
torch==2.7.1+cu121
torchvision==0.22.1+cu121
triton==3.3.1          # Pulled in by PyTorch but pinned to avoid back-tracking.

######## Imaging / CV #########################################################
scikit-image==0.23.2   # Latest wheel that supports NumPy ≥2.2.
pywavelets==1.8.0      # Direct dependency of scikit-image.

######## Single-cell & AnnData ###############################################
anndata==0.11.4
scanpy==1.11.2

######## NLP transformers #####################################################
transformers==4.52.4
