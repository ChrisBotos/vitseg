###############################################################################
# requirements.txt  –  compatible with Python 3.11
# Tested with pip 24.0 on 16 Jun 2025
#
# GPU build (CUDA 12.1):
#     pip install -r requirements.txt \
#        --extra-index-url https://download.pytorch.org/whl/cu121
#
# CPU-only build:
#     pip install -r requirements.txt
###############################################################################

# ---------- Core numerical stack --------------------------------------------
numpy==2.2.0
scipy==1.15.1
pandas==2.3.0

# CuPy binary wheel for CUDA 12.x
cupy-cuda12x==13.5.0

# Lock object used by CuPy
fastrlock>=0.5

# ---------- Arrow / parquet I/O ---------------------------------------------
pyarrow==20.0.0            # 21.x not yet on PyPI for cp311; 20.0.0 is latest wheel.
fastparquet==2024.2.0      # First 2024-series wheel that supports Py3.11.

# ---------- Plotting & imaging ----------------------------------------------
matplotlib==3.10.3
contourpy==1.1.0
pillow==11.2.1
imageio==2.32.1            # Matches scikit-image 0.23 wheels.
scikit-image==0.23.2
pywavelets==1.8.0          # Direct dependency of scikit-image.
imagecodecs==2025.3.30     # Matches scikit-image 0.23 wheels.

# ---------- Machine-learning -------------------------------------------------
scikit-learn==1.7.0
joblib==1.5.1
tqdm==4.67.1

# ---------- Deep-learning stack ---------------------------------------------
# Choose the variant that matches your hardware:
torch==2.7.1+cu121         # GPU wheel (CUDA 12.1)
torchvision==0.22.1+cu121
triton==3.3.1              # Pulled in by torch; pinned to avoid resolver loops.
# --- or comment the three lines above and uncomment the next two for CPU only:
# torch==2.7.1+cpu
# torchvision==0.22.1+cpu

# ---------- Single-cell & AnnData -------------------------------------------
anndata==0.11.4
scanpy==1.11.2

# ---------- NLP / Transformers ---------------------------------------------
transformers==4.52.4

# ---------- Dev / testing ----------------------------------------------------
pytest==8.4.0
